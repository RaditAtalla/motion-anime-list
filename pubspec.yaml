name: motion_anime_list
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.10.4

dependencies:
    build_runner:
    flutter:
        sdk: flutter
    get: ^4.7.3
    hive: ^2.2.3
    hive_flutter: ^1.1.0
    hive_generator:

dev_dependencies:
    flutter_lints: ^6.0.0
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
    assets:
        - assets/
